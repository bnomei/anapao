+++
id = "T001"
title = "Reject unresolved formula state targets"
status = "done"
scope = ["src/validation/mod.rs", "tests/scenario_macro.rs"]
depends = []
covers = ["R001", "R002"]
verification_mode = "required"
verification_status = "passed"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "red-green-refactor"
slice = "normal"
reuse_targets = ["required_target_edge", "compile_scenario_rejects_missing_or_mismatched_state_target_connection"]
read_allowlist = ["src/validation/mod.rs", "src/types/scenario.rs", "tests/scenario_macro.rs", "specs/041-formula-target-validation"]
bundle_with = []
commit = ""
commit_review = ""
implemented_by = "worker:spec041_t001_worker"
verified_by = "validator"
completed_at = "2026-07-13T10:23:03+01:00"
last_lease_id = "l_76299417ced6"
attribution_accepted_paths = [
    "tests/scenario_macro.rs",
]
attribution_reason = "T003 created the formula(absent) regression before this lease; spec 041 T001 explicitly adopted this unchanged in-scope hunk so the checked-builder repair and macro contract land together."
attribution_accepted_by = "validator"
+++

## Context

`StateConnectionTarget::ResourceConnection` and `StateConnectionTarget::StateConnection` call
`required_target_edge` after requiring `target_connection`. The formula branch currently only
requires the ID to be present, allowing an unknown edge ID through compilation. The macro already
delegates scenario construction to the checked builder; preserve that boundary. A partial,
unstaged `target: formula(absent)` regression exists from blocked `040/T003`; this task owns that
in-scope hunk and must preserve and commit it with the builder fix rather than introducing
macro-side validation.

## DoD

- Formula state targets reject an unresolved edge ID through the existing missing-edge error path.
- Native validation coverage includes a `StateConnectionTarget::Formula` case that proves the
  formula branch emits `SetupError::InvalidGraphReference` with the standard scenario-edge
  location and available-ID hint.
- `scenario!` coverage proves `target: formula(absent)` surfaces the same checked-builder error.
- No macro grammar, expansion-hygiene, or macro-side target registry is added.

## Validation

- `cargo fmt --all -- --check`
- `cargo test validation::tests::compile_scenario_rejects_missing_or_mismatched_state_target_connection`
- `cargo test --test scenario_macro`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --all-targets`

## Escalate If

Stop if formula targets intentionally model a different referential domain than scenario edge IDs,
or if matching resource/state diagnostics requires changes outside this task scope.

## Notes

Route: `sol/high` because this restores a cross-module validation invariant and unblocks spec 040.
