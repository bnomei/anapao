# Requirements — 001-pivot-surface

## Goal
Pivot crate surface to testing utility

## EARS
- WHEN implementation starts for 001-pivot-surface THE SYSTEM SHALL satisfy this spec within the declared write scope.
- WHEN dependencies (spec:000-program-control) are not complete THE SYSTEM SHALL keep tasks blocked until dependencies are satisfied.
- WHEN validation runs THE SYSTEM SHALL pass: "cargo check".
