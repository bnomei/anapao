+++
id = "T004"
title = "Complete exports, rustdoc, README, and SemVer guidance"
status = "done"
scope = ["src/scenario_macro.rs", "src/lib.rs", "src/prelude.rs", "README.md", "tests/readme_snippets.rs"]
depends = ["T003"]
covers = ["R001", "R004", "R011", "R012"]
verification_mode = "validator"
verification_status = "passed"
worker_reasoning_effort = "medium"
worker_model = "terra"
test_strategy = "red-green-refactor"
slice = "normal"
reuse_targets = ["src/lib.rs", "src/prelude.rs", "README.md", "tests/readme_snippets.rs"]
read_allowlist = ["src/scenario_macro.rs", "src/lib.rs", "src/prelude.rs", "src/types/scenario_checked.rs", "README.md", "tests/readme_snippets.rs", "tests/scenario_macro.rs", "tests/scenario_macro_ui.rs"]
bundle_with = []
commit = ""
commit_review = ""
implemented_by = "worker:spec040_t004_worker"
verified_by = "validator"
completed_at = "2026-07-13T10:41:58+01:00"
last_lease_id = "l_a6fa061a1d7b"
+++

## Context

Publish the already-proven complete macro without displacing the legacy DTO or direct checked
builder routes.

Canonical macro invocation is `anapao::scenario!`; explicitly `pub use crate::scenario` from the
prelude alongside the cohesive checked-authoring types finalized by spec 039. Do not re-export the
macro from `types`, do not add a named helper macro, and do not duplicate/conflict with the
`#[macro_export]` root item.

Rustdoc, crate docs, README, and `tests/readme_snippets.rs` must teach:

1. the exact queue-flow intake example, unchanged and executable;
2. a complete macro example covering all major sections and representative native/typed escape
   forms without `...`, TODOs, or placeholder families;
3. `Result<Scenario, SetupError>` handling with both `?` and explicit `Err` matching;
4. direct `ScenarioBuilder` equivalence and retained `ScenarioSpec` load/check route;
5. exact symbol-to-ID spelling, separate node/edge namespaces, forward state targets, and node-
   backed tracked metrics;
6. canonical section order, separators, config/mode exclusivity, and complete grammar reference;
7. one evaluation, `$crate`/dependency rename hygiene, no macro-introduced panic, and builder-only
   semantic validation;
8. public 0.2 compatibility: grammar, symbol mapping, evaluation count, result/error types, and
   root/prelude paths cannot be removed/changed compatibly.

Explain that the macro produces the checked `Scenario`; it is not a second serde format or
validation path. Preserve 039's intentional strictness/duplicate migration guidance.

Explicitly state that the deliberate macro set contains one macro. Do not add `expectations!`, an
assertion macro, config/report macros, or implement `Expectation` constructors/
`AssertionReport::assert_success` here. Note normal associated functions and `#[track_caller]` as
the preferred future assertion direction.

Use rustdoc links and examples that compile on MSRV 1.85. `tests/readme_snippets.rs` must pin root
and wildcard-prelude invocation, direct-builder equality, and handled error behavior.

## DoD

- Root and prelude macro paths compile; `types` has no macro re-export.
- Exact and complete examples are executable and contain no elided production surface.
- DTO, checked builder, macro, compile, run, and error roles are clearly separated.
- Grammar, IDs, evaluation, hygiene/rename, no-panic, error layering, and 0.2 SemVer promises are
  documented accurately.
- Docs preserve legacy/direct routes and reject all extra macros/function-scope creep.
- Rustdoc and README snippet tests pin the final public signatures and behavior.

## Validation

```bash
cargo fmt --all -- --check
cargo test --doc
cargo test --test readme_snippets
cargo test --test scenario_macro
cargo test --test scenario_macro_ui
cargo test --test scenario_macro_renamed
cargo check
```

No Docker, network, or external service is required.

## Escalate If

- Root and prelude macro paths conflict or require a duplicate export.
- A complete documented example requires private checked state, raw validation, or an extra macro.
- README syntax differs from the UI/runtime-tested grammar.
- Documentation would promise serde, dynamic IDs, implicit scaling, panic catching, or another
  behavior outside the implemented contract.

## Notes

This task documents the complete final API; it must not resurrect prototype/tracer/MVP language.
